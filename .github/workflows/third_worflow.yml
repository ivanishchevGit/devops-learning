name: Second Workflow
on: push
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List files and copy Repo
        run: ls -a
      - uses: actions/checkout@v7
      - name: List files
        run: ls -a